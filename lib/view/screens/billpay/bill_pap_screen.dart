import 'package:flutter/material.dart';

class BillsPayScreen extends StatelessWidget {
  const BillsPayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bills Pay"),
      ),
    );
  }
}